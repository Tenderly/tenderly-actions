# template.yaml and specs.yaml are required!

files:
  - .gitignore
  - example.ts
  - package.json
  - tsconfig.json

args:
  - name: ACTION_NAME
    description: Name the example actions ([a-z][a-z0-9_-]*)
